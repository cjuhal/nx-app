name: CI
on: [push, pull_request] # Se activa al subir código

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: npm install
      - name: Run tests
        run: npm test # Si esto falla, el "job" se marca como fallido